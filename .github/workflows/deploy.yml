name: Deploy BOTCHA to Cloudflare Workers

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/cloudflare-workers/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Workers
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: packages/cloudflare-workers/package-lock.json
          
      - name: Install dependencies
        run: |
          cd packages/cloudflare-workers
          npm ci
          
      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: packages/cloudflare-workers
          command: deploy
          
      - name: Comment deployment status
        if: github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.sha.substring(0, 7);
            const message = `🚀 BOTCHA deployed to Cloudflare Workers (${sha})
            
            Test the fix:
            \`\`\`bash
            curl -X POST https://botcha.ai/api/verify-landing \\
              -H "Content-Type: application/json" \\
              -d '{"answer": "$(echo -n 'BOTCHA-LANDING-$(date +%Y-%m-%d)' | sha256sum | cut -c1-16)", "timestamp": "$(date -Iseconds)"}'
            \`\`\``;
            
            github.rest.repos.createCommitComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
              body: message
            });